---
id: TASK_0001
state: completed
phase: done
created_at: 2026-09-19T07:35:57.825Z
updated_at: 2026-09-19T08:03:32.406Z
title: Establish `SessionState` as the single in-process owner of YouTube Lounge session state in accordance with Rule R1 of `docs/STATE-TRANSFER-REFACTOR.md`. Done means extracting an immutable data class `SessionState` that owns exactly ordered `playlist`, `current_index`, `current_video_id`, `list_id`, `position`, `duration`, `play_state`, `lane`/`theme`, `volume`, `cpn`, and a monotonic `version` bumped on every accepted change, providing a snapshot accessor that returns an immutable detached copy to all consumers so no external caller can mutate live state, and ensuring existing behavioral gates pass with zero field assignments to these state attributes outside the state owner module.
label: Establish SessionState as single YouTube Lounge state owner
plan_key: P01
---

## raw prompt

Establish SessionState as the single in-process state owner — extract an immutable snapshot data class

## health baseline

```json
{
  "at": "2026-09-19T07:35:57.750Z",
  "treeHash": "f55916d3ab5d7f90f5e7fff1c68fed83b9aa601d",
  "outcome": {
    "ok": true,
    "reason": "no repo-wide static-analysis command found",
    "ecosystem": null,
    "commands": []
  }
}
```

## refined prompt

GOAL
Establish `SessionState` as the single in-process owner of YouTube Lounge session state in accordance with Rule R1 of `docs/STATE-TRANSFER-REFACTOR.md`. Done means extracting an immutable data class `SessionState` that owns exactly ordered `playlist`, `current_index`, `current_video_id`, `list_id`, `position`, `duration`, `play_state`, `lane`/`theme`, `volume`, `cpn`, and a monotonic `version` bumped on every accepted change, providing a snapshot accessor that returns an immutable detached copy to all consumers so no external caller can mutate live state, and ensuring existing behavioral gates pass with zero field assignments to these state attributes outside the state owner module.

CONSTRAINTS
- Restrict scope strictly to Step 1 (Rule R1 from `docs/STATE-TRANSFER-REFACTOR.md`); do not implement the event vocabulary or pure reducer (Step 2 / R3), projection-based player/window management or repair loop deletions (Step 3 / R4), outbound version-diffed publisher batches (Step 4 / R7), multi-channel subscriber fan-out (Step 5 / R9), durable session record persistence (Step 6 / R2), publication-time index derivation (Step 7 / R5), signal confidence tracking or UNKNOWN publishing (Step 8 / R6), tick reconciliation (Step 9 / R8), ad/up-next vocabulary (Step 10 / R10), or the bidirectional reliability matrix test (Step 11).
- `SessionState` must own exactly: ordered `playlist`, `current_index`, `current_video_id`, `list_id`, `position`, `duration`, `play_state`, `lane` / `theme`, `volume`, `cpn`, plus a monotonic integer `version` bumped on each state modification.
- The snapshot accessor must guarantee detachment and immutability (e.g., frozen dataclass and deep-copied or immutable tuple data structures) so readers cannot mutate in-process state.
- Mechanical grep verification must confirm zero assignments to the owned state fields outside the state owner module.
- Existing runtime behavior must remain unchanged and existing test gates must remain green (assertions may inspect snapshots rather than report strings).
- Work strictly inside the provided working checkout directory; do not clone into `/tmp` or alternate paths.

KNOWN-UNKNOWNS
- Is `resources/lib/session_state.py` the mandated canonical module path for `SessionState` (as already imported in `resources/lib/player_bridge.py`), or should it reside under `resources/lib/lounge/`?
- What exact test runner commands and targets constitute "both gates green" in this repository (e.g., `pytest tests/` and an emulator integration suite)?
- How should `lane` vs `theme` be represented in `SessionState`—as a single canonical field (e.g., `lane: Optional[str]` or `theme: str`) or as distinct/aliased fields?
- In the absence of the Step 2 event reducer, what minimal transitional interface should the state owner expose for internal state updates so existing callers in `player_bridge.py` and `session.py` do not break?

EXTERNAL-DEPENDENCIES

## loop events

- 2026-09-19T07:37:47.629Z  worker:files  strike 1/3  stuck in a loop — called grep({"pattern":"KodiPlayerBridge","path":"service.py"}) ×5 in the last 16 calls  → restarted with hint

## verified tooling

./build.sh  build
python3 build_repo_index.py  build
python3 test_addon.py  check
python3 emulator/run_all.py  check

## research

FILES
docs/STATE-TRANSFER-REFACTOR.md:52  Spec defining Rule R1 for single in-process session state owner and snapshot immutability
AGENTS.md:14  Defines mandatory verification test gates (test_addon.py and emulator/run_all.py)
resources/lib/session_state.py:27  Canonical state owner module defining immutable SessionState dataclass and StateOwner snapshot provider
resources/lib/player_bridge.py:180  Kodi player bridge delegating state access to SessionState snapshots and StateOwner
resources/lib/lounge/session.py:86  Lounge session reporting interface consuming playback and queue state attributes
service.py:100  Service entry point wiring StateOwner, KodiPlayerBridge, and LoungeSession instances
tests/test_session_state.py:1  Unit test suite validating SessionState immutability, field ownership, detached snapshots, and monotonic version increments
test_addon.py:141  Fast unit check gate running test_session_state suite
emulator/run_all.py:1  Offline end-to-end integration test runner required to pass as behavior gate

APIS
python3 test_addon.py  Runs fast unit check gate including session state test suite
python3 emulator/run_all.py  Runs offline end-to-end integration test runner behavior gate
SessionState  @dataclass(frozen=True) class SessionState(playlist=(), current_index=0, current_video_id=None, list_id='', position=0.0, duration=0.0, play_state=0, lane=None, volume=100, cpn='kodi', version=0)
SessionState.snapshot  def snapshot(self) -> SessionState
PlayState  class PlayState: STOPPED = 0, PLAYING = 1, PAUSED = 2, UNKNOWN = -1
generate_cpn  def generate_cpn() -> str
StateOwner  class StateOwner(state: SessionState, on_apply: Optional[Callable[[SessionState], None]] = None)
StateOwner.snapshot  def snapshot(self) -> SessionState
StateOwner.apply  def apply(self, event: Event) -> SessionState
StateOwner.playlist  @property def playlist(self) -> Tuple[str, ...]
StateOwner.current_index  @property def current_index(self) -> int
StateOwner.current_video_id  @property def current_video_id(self) -> Optional[str]
StateOwner.list_id  @property def list_id(self) -> str
StateOwner.position  @property def position(self) -> float
StateOwner.duration  @property def duration(self) -> float
StateOwner.play_state  @property def play_state(self) -> int
StateOwner.lane  @property def lane(self) -> Optional[str]
StateOwner.volume  @property def volume(self) -> int
StateOwner.cpn  @property def cpn(self) -> str
StateOwner.version  @property def version(self) -> int
StateOwner.state  @property def state(self) -> int
KodiPlayerBridge  class KodiPlayerBridge(session, resolver=None, stream_selection_type='manual-osd', max_resolution='auto', music_visualizer='auto', initial_state=None, store=None, on_apply=None)
KodiPlayerBridge.owner  owner: StateOwner
KodiPlayerBridge.state  @property def state(self) -> int
KodiPlayerBridge.playlist  @property def playlist(self) -> List[str]
KodiPlayerBridge.current_index  @property def current_index(self) -> int
KodiPlayerBridge.current_video_id  @property def current_video_id(self) -> Optional[str]
KodiPlayerBridge.list_id  @property def list_id(self) -> str
KodiPlayerBridge.current_theme  @property def current_theme(self) -> Optional[str]
KodiPlayerBridge.volume  @property def volume(self) -> int
KodiPlayerBridge.position  @property def position(self) -> float
KodiPlayerBridge.current_duration  @property def current_duration(self) -> int
KodiPlayerBridge.get_time  def get_time(self) -> float
LoungeSession.report_now_playing  def report_now_playing(self, video_id: str, current_time: int, duration: int, state: int, current_index: int = 0, list_id: Optional[str] = None) -> None
LoungeSession.report_now_playing_playlist  def report_now_playing_playlist(self, video_ids: List[str], current_video_id: str, current_index: int, current_time: int, duration: int, state: int, list_id: Optional[str] = None) -> None
LoungeSession.report_state_change  def report_state_change(self, state: int, current_time: int, duration: int) -> None
LoungeSession.report_volume  def report_volume(self, volume: int, muted: bool = False) -> None
run_service  def run_service() -> None
test_session_state_module  def test_session_state_module() -> None
TestSessionState.test_field_specification  def test_field_specification(self) -> None
TestSessionState.test_immutability  def test_immutability(self) -> None
TestSessionState.test_snapshot_immutability_and_detachment  def test_snapshot_immutability_and_detachment(self) -> None

CONTEXT
- resources/lib/session_state.py is the canonical module path for SessionState, already imported by service.py, resources/lib/player_bridge.py, test_addon.py, and tests/test_session_state.py.
- The two required verification test gates specified in AGENTS.md are python3 test_addon.py and python3 emulator/run_all.py.
- test_addon.py executes all test cases in tests/test_session_state.py via unittest.defaultTestLoader.loadTestsFromName("tests.test_session_state").
- SessionState is defined as a @dataclass(frozen=True) in resources/lib/session_state.py.
- SessionState owns exactly eleven fields: playlist, current_index, current_video_id, list_id, position, duration, play_state, lane, volume, cpn, and version.
- SessionState.playlist is typed as Tuple[str, ...] to prevent in-place mutation of the sequence.
- The theme or app identifier ("cl" for YouTube or "m" for YouTube Music) is canonically represented on SessionState as the field lane: Optional[str] = None.
- Transport sessions (LoungeSession) and storage (persistence.py) refer to the lane value under the key or attribute name "theme".
- KodiPlayerBridge provides backward compatibility for the lane field through the read-only property current_theme, which returns self.owner.lane.
- SessionState provides a snapshot() method returning copy.deepcopy(self) for detached state access.
- StateOwner wraps self._state: SessionState and exposes read-only properties for each owned field.
- StateOwner does not currently implement a public snapshot() method delegating to self._state.snapshot().
- KodiPlayerBridge accesses session state exclusively through read-only delegation properties forwarded to self.owner.
- State transitions inside KodiPlayerBridge route through self.owner.apply(event).
- Unit test fixtures in test_addon.py update state without attribute assignment by instantiating a new StateOwner with dataclasses.replace(player.owner._state, **kw).
- There are zero assignments to any of the eleven SessionState field names across resources/lib/ outside resources/lib/session_state.py.
- In resources/lib/lounge/session.py, the assignment self.theme = theme assigns to the transport session instance rather than session state.
- StateOwner.__init__ accepts an optional on_apply callback invoked whenever new state is set.

VERIFIED-TOOLING
  ./build.sh
  python3 build_repo_index.py
  python3 test_addon.py
  python3 emulator/run_all.py

## grill Q&A

(no questions produced)

## spec

GOAL
Establish `SessionState` in `resources/lib/session_state.py` as the single in-process owner of YouTube Lounge session state in accordance with Rule R1 of `docs/STATE-TRANSFER-REFACTOR.md`. Done means extracting an immutable data class `SessionState` that owns exactly ordered `playlist`, `current_index`, `current_video_id`, `list_id`, `position`, `duration`, `play_state`, `lane`/`theme`, `volume`, `cpn`, and a monotonic `version` bumped on every accepted change, providing a snapshot accessor on `StateOwner` that returns an immutable detached copy to all consumers so no external caller can mutate live state, and ensuring existing behavioral gates pass with zero field assignments to these state attributes outside the state owner module.

CONSTRAINTS
- Restrict scope strictly to Step 1 (Rule R1 from `docs/STATE-TRANSFER-REFACTOR.md`); do not implement the event vocabulary or pure reducer (Step 2 / R3), projection-based player/window management or repair loop deletions (Step 3 / R4), outbound version-diffed publisher batches (Step 4 / R7), multi-channel subscriber fan-out (Step 5 / R9), durable session record persistence (Step 6 / R2), publication-time index derivation (Step 7 / R5), signal confidence tracking or UNKNOWN publishing (Step 8 / R6), tick reconciliation (Step 9 / R8), ad/up-next vocabulary (Step 10 / R10), or the bidirectional reliability matrix test (Step 11). [from: spec]
- `SessionState` must own exactly: ordered `playlist`, `current_index`, `current_video_id`, `list_id`, `position`, `duration`, `play_state`, `lane` / `theme`, `volume`, `cpn`, plus a monotonic integer `version` bumped on each state modification. [from: spec]
- The snapshot accessor must guarantee detachment and immutability (e.g., frozen dataclass and deep-copied or immutable tuple data structures) so readers cannot mutate in-process state. [from: spec]
- Mechanical grep verification must confirm zero assignments to the owned state fields outside the state owner module. [from: spec]
- Existing runtime behavior must remain unchanged and existing test gates must remain green (assertions may inspect snapshots rather than report strings). [from: spec]
- Work strictly inside the provided working checkout directory; do not clone into `/tmp` or alternate paths. [from: spec]
- Canonical state owner code must reside in `resources/lib/session_state.py` with `StateOwner` exposing a public `snapshot()` method returning an immutable detached `SessionState`.
- Canonical state representation for the app/theme identifier is `lane: Optional[str]`, with backward-compatibility properties (such as `current_theme`) preserved for callers.

ACCEPTANCE
- `resources/lib/session_state.py` defines frozen dataclass `SessionState` containing exactly the 11 fields: `playlist`, `current_index`, `current_video_id`, `list_id`, `position`, `duration`, `play_state`, `lane`, `volume`, `cpn`, and `version`.
- `StateOwner.snapshot()` returns an immutable, detached `SessionState` snapshot that prevents callers from altering live in-process state.
- `StateOwner` increments `version` monotonically on every accepted state transition.
- Zero field assignments to the 11 session state attributes occur across `resources/lib/` outside `resources/lib/session_state.py`.
- Fast unit test gate passes cleanly via `python3 test_addon.py`.
- Offline integration runner passes cleanly via `python3 emulator/run_all.py`.

VERIFY:
```sh
python3 test_addon.py
python3 emulator/run_all.py
```

## phase timings

attempt 1

refine               24.7s
research            301.9s
  enrichment             330ms
  worker:files wait      366ms
  worker:files work      45.9s
  worker:files discarded (1 restart)   34.5s
  worker:apis wait       492ms
  worker:apis work       67.4s
  worker:context wait    366ms
  worker:context work     4.4s
  worker:context wait    366ms
  worker:context work    94.8s
  worker:tooling wait    367ms
  worker:tooling work    37.0s
  workers               286.1s
  verify-tooling         15.7s
grill                 6.5s
  gen                     6.5s
compose               9.5s
critique             12.2s
  triage                 12.2s
total               354.8s

## handoff

handoff_at: 2026-09-19T07:41:56.659Z
attempt: 1
spec_hash: d2c3ade17b24
delivered: fresh

## gates

- 2026-09-19T08:03:29.509Z verify: PASS
- 2026-09-19T08:03:29.530Z commit: task snapshot committed
- 2026-09-19T08:03:32.404Z enforce(edit): clean
- 2026-09-19T08:03:32.405Z enforce(edit): no code edits — enforce commit and re-verify skipped

